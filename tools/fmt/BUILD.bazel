load("@rules_rust//rust:defs.bzl", "rust_binary")

package(default_visibility = ["//visibility:public"])

rust_binary(
    name = "twigz-fmt",
    srcs = ["src/main.rs"],
    crate_name = "twigz_fmt",
    edition = "2021",
    deps = [
        "//crates/dsl",
        "//crates/format",
    ],
)
